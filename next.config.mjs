/** @type {import('next').NextConfig} */
export default {
  // Nothing to configure. No image optimisation (there are no images), no
  // redirects, no analytics, no instrumentation. See invariants S2 and S5.
  poweredByHeader: false,
};
